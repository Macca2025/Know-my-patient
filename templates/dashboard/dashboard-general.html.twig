{# =============================
   General Dashboard Section (for all users)
   ============================= #}

<div class="dashboard-card-modern card-info">
    <div class="card-body">
        <div class="card-icon">
            <i class="bi bi-person-circle text-primary"></i>
        </div>
        <h5 class="card-title">My Profile</h5>
        <p class="card-text">View and update your personal account settings.</p>
        <div class="card-actions">
            <a href="{{ url_for('my_profile') }}" class="btn btn-outline-primary">
                <i class="bi bi-pencil me-2"></i>My Profile
            </a>
        </div>
        <div class="card-footer-info">
            <small><i class="bi bi-pencil"></i>My Information</small>
        </div>
    </div>
</div>

<div class="dashboard-card-modern card-info">
    <div class="card-body">
        <div class="card-icon">
            <i class="bi bi-shield-lock text-info"></i>
        </div>
        <h5 class="card-title">Privacy &amp; Security</h5>
        <p class="card-text">Control your privacy settings and security settings.</p>
        <div class="card-actions">
            <a href="{{ url_for('privacy_policy')}}" class="btn btn-outline-info">
                <i class="bi bi-gear me-2"></i>Privacy Settings
            </a>
        </div>
        <div class="card-footer-info">
            <small><i class="bi bi-shield-lock"></i>Privacy Settings</small>
        </div>
    </div>
</div>

<div class="dashboard-card-modern card-danger">
        <div class="card-body">
                <div class="card-icon">
                        <i class="bi bi-trash text-danger"></i>
                </div>
                <h5 class="card-title">Delete Account</h5>
                <p class="card-text">Permanently delete your account and all associated records.</p>
                <div class="card-actions">
                        <a  class="btn btn-outline-danger" href="{{ url_for('confirm_deletion') }}">
                                <i class="bi bi-exclamation-triangle me-2"></i>Delete Account
                        </a>
                </div>
                <div class="card-footer-info">
                        <small><i class="bi bi-exclamation-triangle"></i>Irreversible Action</small>
                </div>
        </div>
</div>

{# Immediate delete-account modal removed. Users should use the
   /confirm-deletion flow (linked from other parts of the UI) which
   requires typing the confirmation phrase. #}
